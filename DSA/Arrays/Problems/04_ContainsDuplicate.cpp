/*
Problem: Contains Duplicate

Given an integer array `nums`, return `true` if any value appears at least twice in the array, and return `false` if every element is distinct.

Example 1:
Input: nums = [1,2,3,1]
Output: true

Example 2:
Input: nums = [1,2,3,4]
Output: false

Example 3:
Input: nums = [1,1,1,3,3,4,3,2,4,2]
Output: true

Constraints:
1 <= nums.length <= 10^5
-10^9 <= nums[i] <= 10^9

Hints:
1. How can you efficiently keep track of elements you've seen so far?
2. Consider sorting the array first. What would that tell you about duplicates?
3. What data structure offers average O(1) time complexity for insertions and lookups?
*/

#include <vector>
#include <iostream>

// You will write your code here
// bool containsDuplicate(std::vector<int>& nums) {
//     
// }

int main() {
    // Example usage and testing (optional)
    // std::vector<int> nums = {1, 2, 3, 1};
    // if (containsDuplicate(nums)) {
    //     std::cout << "Contains duplicate." << std::endl;
    // } else {
    //     std::cout << "Does not contain duplicate." << std::endl;
    // }
    return 0;
}
