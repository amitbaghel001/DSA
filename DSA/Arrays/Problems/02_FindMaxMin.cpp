/*
Problem: Find Maximum and Minimum Elements in an Array

Given an array `A` of size `N`, find the maximum and minimum elements in the array.

Example 1:
Input: A = [3, 2, 1, 5, 4], N = 5
Output: Max = 5, Min = 1

Example 2:
Input: A = [8, 8, 8, 8, 8], N = 5
Output: Max = 8, Min = 8

Example 3:
Input: A = [10], N = 1
Output: Max = 10, Min = 10

Constraints:
1 <= N <= 10^5
-10^9 <= A[i] <= 10^9

Hints:
1. Initialize max and min with the first element of the array.
2. Iterate through the rest of the array, comparing each element with the current max and min.
3. Can you do it in a single pass?
*/

#include <vector>
#include <iostream>
#include <algorithm> // For std::max and std::min

// You will write your code here
// struct MinMaxResult {
//     int minVal;
//     int maxVal;
// };
//
// MinMaxResult findMinMax(const std::vector<int>& arr) {
//     
// }

int main() {
    // Example usage and testing (optional)
    // std::vector<int> arr = {3, 2, 1, 5, 4};
    // MinMaxResult result = findMinMax(arr);
    // std::cout << "Max: " << result.maxVal << ", Min: " << result.minVal << std::endl;
    return 0;
}
