/*
Problem: Move Zeroes

Given an integer array `nums`, move all `0`'s to the end of it while maintaining the relative order of the non-zero elements.

Note that you must do this in-place without making a copy of the array.

Example 1:
Input: nums = [0,1,0,3,12]
Output: [1,3,12,0,0]

Example 2:
Input: nums = [0]
Output: [0]

Constraints:
1 <= nums.length <= 10^4
-2^31 <= nums[i] <= 2^31 - 1

Hints:
1. Use two pointers.
2. One pointer can track the position to place the next non-zero element.
3. The other pointer can iterate through the array to find non-zero elements.
*/

#include <vector>
#include <iostream>

// You will write your code here
// void moveZeroes(std::vector<int>& nums) {
//     
// }

int main() {
    // Example usage and testing (optional)
    // std::vector<int> nums = {0,1,0,3,12};
    // moveZeroes(nums);
    // std::cout << "Array after moving zeroes: ";
    // for (int num : nums) {
    //     std::cout << num << " ";
    // }
    // std::cout << std::endl;
    return 0;
}
