class Solution {
public:
    int hammingWeight(int n) {
        int count=0;
        while(n>0){
            int bits=n & 1;
            if(bits==1) count++;
            n>>=1;
        }
        return count;
    }
};
